from sympy import symbols, lambdify, sqrt, pi

# Define symbols
MaxV, AMaxV, LSA, TLS, Thickness, TC, RC, Height, GE, fin_area, fin_Cx, Temp, Spd_of_Sound, airP, etc. = symbols('MaxV AMaxV LSA TLS Thickness TC RC Height GE fin_area fin_Cx Temp Spd_of_Sound airP etc.')

# Define expressions for each calculation
# Example: Fin_Eps_expr, ThicknessRatio_expr, Lambda_expr, AspectRatio_expr, etc.
